import type { ParseOptions, ParseResult } from '../types/index.js';

/**
 * Parses LRC source text into an editable {@link LrcDocument} plus diagnostics.
 * Stub: pipeline stage not yet implemented.
 */
export function parseLrc(_text: string, _options: ParseOptions): ParseResult {
  throw new Error('parseLrc: not implemented');
}
